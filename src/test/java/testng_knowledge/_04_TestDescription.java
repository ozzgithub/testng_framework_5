package testng_knowledge;

import org.testng.annotations.Ignore;
import org.testng.annotations.Test;

public class _04_TestDescription {
/*
    @Test(priority = 1, description = "Login with valid credentials")
    public void login(){
        System.out.println("This is login");
    }


    @Ignore // This is for ignore the test. If you don't want to execute the test.
    @Test(priority = 2, description = "Login valid with invalid credentials")
    public void invalidLogin(){
        System.out.println("This is invalid login");
    }

 */
}
